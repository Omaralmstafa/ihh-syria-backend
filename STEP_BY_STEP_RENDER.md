# خطوات النشر على Render - خطوة بخطوة مع لقطات الشاشة

## ⚠️ ملاحظة مهمة

لا يمكنني الوصول إلى حساب Render الخاص بك، لكن يمكنني إرشادك خطوة بخطوة.

## 📋 قبل البدء

تأكد من:
- ✅ لديك حساب على Render (يمكن التسجيل بـ GitHub)
- ✅ الواجهة موجودة على GitHub
- ✅ `package.json` موجود في المستودع

## 🚀 الخطوات التفصيلية

### الخطوة 1: الدخول إلى Render

1. اذهب إلى [render.com](https://render.com)
2. اضغط **Sign In** أو **Get Started for Free**
3. سجل دخول باستخدام GitHub (أسهل طريقة)

### الخطوة 2: إنشاء Static Site

1. بعد تسجيل الدخول، ستجد Dashboard
2. اضغط على **New +** في أعلى الصفحة (أعلى يسار)
3. من القائمة المنسدلة، اختر **Static Site**

### الخطوة 3: ربط المستودع

#### إذا لم تكن مرتبطاً بـ GitHub:

1. ستظهر نافذة "Connect a repository"
2. اضغط **Connect GitHub** (أو GitLab/Bitbucket)
3. سيعيد توجيهك إلى GitHub للموافقة
4. اضغط **Authorize Render**
5. سيعيدك إلى Render

#### بعد الربط:

1. ستظهر قائمة المستودعات
2. ابحث عن مستودع الواجهة (مثل `ihh-syria-frontend`)
3. اضغط على المستودع

### الخطوة 4: إعدادات النشر

بعد اختيار المستودع، ستظهر صفحة الإعدادات:

#### 4.1 المعلومات الأساسية:

```
Name: ihh-syria-frontend
     (أو أي اسم تريده - سيستخدم في الرابط)
```

#### 4.2 Branch:

```
Branch: main
      (أو master حسب فرعك)
```

#### 4.3 Root Directory:

**هذا مهم جداً!**

- إذا كان `package.json` في **جذر المستودع**: اتركه **فارغاً**
- إذا كان في مجلد `frontend`: اكتب `frontend`
- إذا كان في مجلد آخر: اكتب اسم المجلد

**للتحقق:**
- اذهب إلى GitHub
- افتح المستودع
- ابحث عن `package.json`
- انظر في أي مجلد هو

#### 4.4 Build Command:

```
npm install && npm run build
```

#### 4.5 Publish Directory:

```
build
```

(أو `dist` إذا كنت تستخدم Vite)

### الخطوة 5: Environment Variables (اختياري)

1. اضغط **Advanced** في أسفل الصفحة
2. اضغط **Add Environment Variable**
3. أضف:
   - **Key**: `REACT_APP_API_URL`
   - **Value**: `https://ihh-syria-backend.onrender.com`
     (استبدل `ihh-syria-backend` باسم Backend الفعلي)

### الخطوة 6: النشر

1. راجع جميع الإعدادات
2. اضغط **Create Static Site**
3. انتظر اكتمال البناء (2-5 دقائق)

### الخطوة 7: التحقق من النشر

بعد اكتمال البناء:

1. ستجد رابط في أعلى الصفحة مثل:
   ```
   https://ihh-syria-frontend.onrender.com
   ```
2. اضغط على الرابط للتحقق
3. يجب أن ترى الواجهة تعمل

## 🔧 إذا واجهت مشكلة package.json

### الخطأ:
```
npm error enoent Could not read package.json
```

### الحل:

1. في Render Dashboard
2. اضغط على Static Site
3. اضغط **Settings**
4. ابحث عن **Root Directory**
5. غيّره حسب موقع `package.json`:
   - إذا كان في الجذر: اتركه فارغاً
   - إذا كان في `frontend`: اكتب `frontend`
6. اضغط **Save Changes**
7. اضغط **Manual Deploy** > **Clear build cache & deploy**

## 📝 مثال: إعدادات صحيحة

### السيناريو 1: package.json في الجذر

```
repository/
├── package.json  ← هنا
├── src/
└── public/
```

**الإعدادات:**
- Name: `ihh-syria-frontend`
- Branch: `main`
- Root Directory: (فارغ)
- Build Command: `npm install && npm run build`
- Publish Directory: `build`

### السيناريو 2: package.json في مجلد frontend

```
repository/
├── frontend/
│   ├── package.json  ← هنا
│   └── src/
└── README.md
```

**الإعدادات:**
- Name: `ihh-syria-frontend`
- Branch: `main`
- Root Directory: `frontend`
- Build Command: `npm install && npm run build`
- Publish Directory: `build`

## ✅ بعد النشر الناجح

### 1. تحديث CORS في Backend

1. اذهب إلى Backend Service على Render
2. اضغط **Environment**
3. ابحث عن `CORS_ORIGINS`
4. غيّره إلى:
   ```
   https://ihh-syria-frontend.onrender.com
   ```
   (أو أضفها إذا لم تكن موجودة)
5. اضغط **Save Changes**

### 2. اختبار التكامل

1. افتح الواجهة: `https://ihh-syria-frontend.onrender.com`
2. جرب تسجيل الدخول
3. افتح Developer Tools (F12)
4. تحقق من عدم وجود أخطاء CORS

## 📸 ما ستراه في Render

### صفحة الإعدادات:

```
┌─────────────────────────────────────────┐
│  New Static Site                        │
├─────────────────────────────────────────┤
│  Connect a repository                   │
│  [GitHub ▼]                             │
│                                         │
│  Select repository:                    │
│  [▼ ihh-syria-frontend]                 │
│                                         │
│  Name: [ihh-syria-frontend]            │
│  Branch: [main]                         │
│  Root Directory: [        ]            │
│                                         │
│  Build Command:                         │
│  [npm install && npm run build]        │
│                                         │
│  Publish Directory:                     │
│  [build]                                │
│                                         │
│  [Advanced ▼]                           │
│  Environment Variables:                 │
│  REACT_APP_API_URL = ...               │
│                                         │
│  [Create Static Site]                  │
└─────────────────────────────────────────┘
```

## 🆘 إذا احتجت مساعدة

1. تحقق من Build Logs في Render
2. راجع `RENDER_BUILD_FIX.md` لحل المشاكل
3. تأكد من أن `package.json` موجود في Git
4. تحقق من Root Directory

## 💡 نصائح

- ✅ استخدم GitHub للربط (أسهل)
- ✅ تحقق من موقع `package.json` قبل النشر
- ✅ استخدم **Clear build cache** إذا استمرت المشاكل
- ✅ راجع Build Logs دائماً

## 🎯 النتيجة النهائية

بعد النشر الناجح:
- ✅ Frontend: `https://ihh-syria-frontend.onrender.com`
- ✅ Backend: `https://ihh-syria-backend.onrender.com`
- ✅ كل شيء يعمل معاً!


