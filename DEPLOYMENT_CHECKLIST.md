# قائمة فحص النشر - Deployment Checklist

## ✅ التحقق من الجاهزية

### 1. ملفات التكوين الأساسية
- [x] `requirements.txt` - يحتوي على جميع المكتبات المطلوبة
- [x] `Procfile` - أمر تشغيل التطبيق
- [x] `render.yaml` - تكوين Render (اختياري)
- [x] `runtime.txt` - إصدار Python
- [x] `.gitignore` - استبعاد الملفات غير الضرورية
- [x] `README.md` - دليل المشروع
- [x] `DEPLOYMENT.md` - دليل النشر

### 2. الكود الرئيسي
- [x] `src/main.py` - نقطة الدخول الرئيسية
- [x] جميع الـ blueprints مسجلة بشكل صحيح
- [x] متغيرات البيئة مستخدمة بشكل صحيح
- [x] قاعدة البيانات متصلة (PostgreSQL للإنتاج)
- [x] CORS مُعد بشكل صحيح

### 3. Routes (المسارات)
- [x] `routes/auth.py` - المصادقة
- [x] `routes/user.py` - إدارة المستخدمين (محدث)
- [x] `routes/media.py` - الوسائط (يدعم PDF)
- [x] `routes/office.py` - إدارة المكاتب
- [x] `routes/pdf.py` - توليد PDF (جديد)

### 4. Models (النماذج)
- [x] `models/user.py` - نموذج المستخدم (يحتوي على profile_image_url)
- [x] جميع العلاقات صحيحة

### 5. Utilities (الأدوات)
- [x] `utils/i18n.py` - نظام متعدد اللغات
- [x] جميع الملفات `__init__.py` موجودة

### 6. المكتبات المطلوبة
- [x] Flask
- [x] flask-cors
- [x] flask-sqlalchemy
- [x] flask-jwt-extended
- [x] gunicorn (للإنتاج)
- [x] python-dotenv
- [x] reportlab (لـ PDF)
- [x] pandas, openpyxl

### 7. الميزات الجديدة
- [x] تعديل بيانات المتطوع (جميع الحقول)
- [x] دعم تحميل PDF
- [x] نظام متعدد اللغات (عربي/إنجليزي/تركي)
- [x] حفظ صورة المستخدم في قاعدة البيانات
- [x] توليد PDF بالعربية

## 📋 خطوات النشر على Render

### الخطوة 1: إعداد Git Repository
```bash
cd ihh_syria_project_final
git init
git add .
git commit -m "Ready for deployment"
git remote add origin <your-repo-url>
git push -u origin main
```

### الخطوة 2: إنشاء حساب Render
1. اذهب إلى [render.com](https://render.com)
2. سجل حساب جديد (يمكن استخدام GitHub)
3. اربط حساب GitHub/GitLab

### الخطوة 3: إنشاء قاعدة بيانات PostgreSQL
1. في Render Dashboard: **New +** > **PostgreSQL**
2. الإعدادات:
   - Name: `ihh-syria-db`
   - Database: `ihh_syria`
   - Plan: Free (للبداية)
3. اضغط **Create Database**

### الخطوة 4: إنشاء Web Service

#### الطريقة الأولى: استخدام render.yaml (موصى به)
1. **New +** > **Blueprint**
2. اختر المستودع الخاص بك
3. Render سيقوم بإنشاء كل شيء تلقائياً
4. راجع الإعدادات واضغط **Apply**

#### الطريقة الثانية: النشر اليدوي
1. **New +** > **Web Service**
2. اختر المستودع
3. الإعدادات:
   - **Name**: `ihh-syria-backend`
   - **Environment**: `Python 3`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `gunicorn --bind 0.0.0.0:$PORT home.ubuntu.ihh_syria_backend.src.main:app`
   - **Plan**: Free (للبداية)

### الخطوة 5: إعداد متغيرات البيئة
في قسم **Environment Variables**:
- `SECRET_KEY` - سيتم إنشاؤه تلقائياً (إذا استخدمت render.yaml)
- `JWT_SECRET_KEY` - سيتم إنشاؤه تلقائياً (إذا استخدمت render.yaml)
- `FLASK_ENV` = `production`
- `CORS_ORIGINS` = `*` (أو قائمة بالمواقع المسموحة)
- `DATABASE_URL` - سيتم إضافته تلقائياً عند ربط قاعدة البيانات

### الخطوة 6: ربط قاعدة البيانات
- في قسم **Database**، اختر `ihh-syria-db`
- سيتم إضافة `DATABASE_URL` تلقائياً

### الخطوة 7: النشر
- اضغط **Create Web Service**
- انتظر اكتمال البناء (قد يستغرق 5-10 دقائق)

### الخطوة 8: التحقق
1. افتح رابط التطبيق
2. جرب: `https://your-app.onrender.com/health`
3. يجب أن ترى: `{"status": "healthy", "database": "connected"}`

## ⚠️ ملاحظات مهمة قبل النشر

### 1. الأمان
- [ ] تأكد من تغيير `SECRET_KEY` و `JWT_SECRET_KEY` في الإنتاج
- [ ] لا تشارك متغيرات البيئة مع أي شخص
- [ ] استخدم HTTPS دائماً

### 2. قاعدة البيانات
- [ ] SQLite للمطورين المحليين فقط
- [ ] PostgreSQL للإنتاج على Render
- [ ] قم بعمل نسخة احتياطية من قاعدة البيانات

### 3. الملفات المرفوعة
- [ ] على Render Free Plan، الملفات قد تُحذف عند إعادة التشغيل
- [ ] فكر في استخدام خدمة تخزين خارجية (AWS S3) للإنتاج

### 4. الأداء
- [ ] Free Plan قد يكون بطيئاً في البداية (Cold Start ~30 ثانية)
- [ ] فكر في الترقية إلى Paid Plan للإنتاج

### 5. خطوط العربية في PDF
- [ ] أضف ملف خط عربي في `src/routes/pdf.py` في متغير `ARABIC_FONT_PATH`
- [ ] أو استخدم خطوط Google Fonts (Noto Sans Arabic)

## 🔍 التحقق من النشر

بعد النشر، تحقق من:

1. **Health Check**:
   ```
   GET https://your-app.onrender.com/health
   Response: {"status": "healthy", "database": "connected"}
   ```

2. **API Endpoints**:
   ```
   GET https://your-app.onrender.com/
   Response: {"message": "IHH Syria API is running!", "status": "success"}
   ```

3. **Authentication**:
   ```
   POST https://your-app.onrender.com/api/auth/register
   POST https://your-app.onrender.com/api/auth/login
   ```

4. **File Upload**:
   ```
   POST https://your-app.onrender.com/api/media/upload
   ```

5. **PDF Generation**:
   ```
   GET https://your-app.onrender.com/api/pdf/user/1?lang=ar
   ```

## 📞 الدعم

إذا واجهت مشاكل:
1. راجع Logs في Render Dashboard
2. تحقق من أن جميع متغيرات البيئة موجودة
3. تأكد من أن قاعدة البيانات مربوطة
4. راجع `DEPLOYMENT.md` للتعليمات التفصيلية

## ✅ المشروع جاهز للنشر!

جميع الملفات موجودة ومحدثة. يمكنك الآن:
1. رفع المشروع إلى Git
2. نشره على Render
3. البدء في الاستخدام!

